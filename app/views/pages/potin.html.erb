<header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
        <span class="fs-4">The Gossip Project</span>
      </a>

      <ul class="nav nav-pills">
        <li class="nav-item"><a href="#" class="nav-link active" aria-current="page">Home</a></li>
        <li class="nav-item"><a href=<%= contact_path %> class="nav-link">Contact</a></li>
        <li class="nav-item"><a href=<%= team_path %> class="nav-link">Team</a></li>
      </ul>
    </header>


  <% potin = Gosssip.find(params[:id]) %>

<div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold"><%= potin.user.last_name %> <%= potin.user.first_name %></h1>
        <p class="col-md-8 fs-4">Titre du potin: <%= potin.title  %></p>
        <p class="col-md-8 fs-4">Contenue du potin: <%= potin.content  %></p>
        <p class="col-md-8 fs-4">Le potin a été crée le: <%= potin.created_at  %></p>
        <button class="btn btn-primary btn-lg" type="button" ><a href="/user/<%= @get_gossip.user.id %>>"> Lien du profil </a></button>
      </div>
    </div>