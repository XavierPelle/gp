<header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
        <span class="fs-4">The Gossip Project</span>
      </a>

      <ul class="nav nav-pills">
        <li class="nav-item"><a href="#" class="nav-link active" aria-current="page">Home</a></li>
        <li class="nav-item"><a href=<%= contact_path %> class="nav-link">Contact</a></li>
        <li class="nav-item"><a href=<%= team_path %> class="nav-link">Team</a></li>
      </ul>
    </header>


    <p> Name : <%= @get_user.first_name %> </p>
    <p> Last Name : <%= @get_user.last_name %> </p>
    <p> Email : <%= @get_user.email %> </p>
    <p> Age : <%= @get_user.age %> </p>
    

    

    <% @tab.each do |i| %>
        <p> --------------------------------</p>
        <p> Gossip n°<%= i.id  %> / Title : <%= i.title %> </p>
        <p> Content : <%= i.content %> </p>
    <% end %>